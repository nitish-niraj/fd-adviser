# Static India Map Asset

Place your custom India map image here as:

- `assets/india-map.png`

Supported alternatives:

Current UI is configured to use `assets/india-map.png`.
If the file is missing, the app falls back to the built-in interactive SVG map.
